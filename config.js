// UK Live Transit
// Now powered by bustimes.org – no API keys required.
// Be polite: the app already debounces map moves.

const CONFIG = {
  // Optional future keys (not needed for basic use)
  // BODS_API_KEY: "",
  // TRANSPORTAPI_APP_ID: "",
  // TRANSPORTAPI_APP_KEY: ""
};
